apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This file references the manifests rendered from the Helm chart.
# Regenerate manifests.yaml by running: make render
# (or: scripts/render.sh)
resources:
  - manifests.yaml
